[project]
name = "gitbard"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "requests",
    "python-dotenv",
    "httpx>=0.28.1",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
